"use client";

// Grain overlay removed — gloss black surfaces replace grain texture.
// Component kept as a null export so existing imports don't break.
export function GrainOverlay() {
  return null;
}
